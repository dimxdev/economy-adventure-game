import { LevelScene } from './LevelScene.js';

/** Level 1 — Misi Pengamatan (isi mekanik: FASE 4). */
export class Level1Scene extends LevelScene {
  constructor() {
    super('Level1', 'level1', 'bg_level1');
  }
}
