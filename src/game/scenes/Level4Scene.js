import { LevelScene } from './LevelScene.js';

/** Level 4 — Jalur Distribusi (isi mekanik: FASE 7). */
export class Level4Scene extends LevelScene {
  constructor() {
    super('Level4', 'level4', 'bg_level4');
  }
}
